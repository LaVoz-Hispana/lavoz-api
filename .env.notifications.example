# Merge these into the existing API/worker environment. Do not commit secrets.
NOTIFICATION_EMAIL_ENABLED=false
CLIENT_URL=https://www.poststation.link
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
# Existing MY_EMAIL and APP_PASSWORD remain supported as fallbacks.
# The worker uses the same DB_HOST, DB_USER, DB_PASSWORD, DB_NAME, DB_CHARSET as the API.
